<%= form_for(@trip, :html => { :class => "form-horizontal"}) do |f| %>
  <% if @trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trip.errors.count, "error") %> prohibited this trip from being saved:</h2>

      <ul>
      <% @trip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="control-group">
    <%= f.label :place %>
    <div class="controls"><%= f.text_field :place %></div>
</div>

<div class="control-group">
    <%= f.label :start_date %>
    <div class="controls"><%= f.text_field :start_date %></div>
  </div>
  <div class="control-group">
      <%= f.label :start_time %>
      <div class="controls"><%= f.text_field :start_time %></div>
    </div>
  <div class="control-group">
    <%= f.label :end_time %>
    <div class="controls"><%= f.text_field :end_time %></div>
  </div>
  <div class="control-group">
    <%= f.label :description %>
    <div class="controls"><%= f.text_area(:description, :size => "40x3") %></div>
  </div>
  <div class="button">
    <%= f.submit 'Add Trip', :class => 'btn primary' %>
  </div>
<% end %>
